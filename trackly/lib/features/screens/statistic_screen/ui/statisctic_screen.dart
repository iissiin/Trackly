import 'package:flutter/material.dart';

class StatiscticScreen extends StatelessWidget {
  const StatiscticScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('СТАтистика')));
  }
}
